namespace EList.Models.Enums
{
    /// <summary>
    /// Системный тип альбома события (не редактируется пользователем).
    /// </summary>
    public enum EventAlbumSystemKind : short
    {
        DiscussionPhotos = 1
    }
}
